router:
  http.get:
    rco-nodeca.contacts:
      "/contacts": ~

    rco-nodeca.contacts.sales:
      "/contacts/sales": ~

    rco-nodeca.contacts.support:
      "/contacts/support": ~

    rco-nodeca.contacts.nick_change:
      "/contacts/nick_change": ~

    rco-nodeca.contacts.user_update:
      "/contacts/user_update": ~

    rco-nodeca.contacts.mod_appeal:
      "/contacts/mod_appeal": ~

    admin.rco-nodeca.update_by_link:
      "/members/update_by_link/{user_hid}":
        user_hid:
          match: /\d+/
          type: 'integer'
