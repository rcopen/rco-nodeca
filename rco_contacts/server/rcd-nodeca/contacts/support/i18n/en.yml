en-US:
  exec:
    email_subject: '#{project_name}: Вопрос администраторам форума'

    email_text_guest: |
      Кто:   #{name}
      Email: #{email}

      Сообщение:
      ---------------------------
      #{message}

    email_text_member: |
      Пользователь: #{name}, #{profile}

      Сообщение:
      ---------------------------
      #{message}

    err_wrong_captcha: CAPTCHA verification failed, try again.
