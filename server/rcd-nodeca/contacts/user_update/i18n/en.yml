en-US:
  exec:
    email_subject: '#{project_name}: Исправление данных профиля — #{username}'

    email_text: |
      Пользователь: #{name}
      URL: #{profile}
      Имя: #{firstname}
      Фамилия: #{lastname}
      Дата рождения: #{birthday}

      #{link}
