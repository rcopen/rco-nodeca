en-US:
  exec:
    email_subject: '#{project_name}: Жалоба на модератора'

    email_text: |
      Пользователь: #{name}
      URL: #{profile}

      Сообщение:
      ---------------------------
      #{message}

    err_wrong_captcha: CAPTCHA verification failed, try again.
