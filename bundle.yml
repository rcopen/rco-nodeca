bundles:
  frontend:
    - rcd-nodeca


packages:
  admin:
    entries:
      - server/admin

  common:
    entries:
      - client/common

  users:
    entries:
      - client/users
      - internal/users

  rcd-nodeca: {}
